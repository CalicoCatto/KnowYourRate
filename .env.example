# Database
DB_PASSWORD=changeme
DATABASE_URL=postgresql+asyncpg://kyr:changeme@db:5432/knowyourrate

# Encryption key for storing API keys (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_SECRET=generate-a-real-secret

# Optional: YouTube Data API key (for automatic channel lookup)
# Get yours at https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=
